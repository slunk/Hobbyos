#ifndef _STRING_H
#define _STRING_H
#include <stdbool.h>
#include <stddef.h>
#include <stdarg.h>
#include <system.h>

size_t strlen(const char *str);
char *strcpy(char *destination, const char *source);
#endif
